package com.nacs.recursion.main;

import java.util.Random;

import com.nacs.recursion.Power;

public class MainPower {
    public static void main(String[] args) {
//        Random random = new Random();
//  *+
//        // Generate random base (1 to 10) and exponent (0 to 5)
//        int base = random.nextInt(10) + 1;
//        int exponent = random.nextInt(6);
//
//        int result = Power.calculatePower(base, exponent);
//        System.out.println("Base: " + base);
//        System.out.println("Exponent: " + exponent);
//        System.out.println(base + "^" + exponent + " = " + result);
    	System.out.println(Power.calculatePower(5, 3));
    }
}
